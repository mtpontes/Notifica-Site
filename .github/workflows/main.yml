name: Build and Deploy serverless functions


on:
  push:
    branches: [ "master", "slsv4" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit

  deploy:
    needs: build
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
